// Este archivo puede usarse para exponer APIs seguras al renderer
// Por ahora, lo dejaremos vacío.

window.addEventListener('DOMContentLoaded', () => {
  console.log('Preload script cargado');
});
